// Variables y Selectores
const formulario = document.getElementById('calcular-notas');
const notas = document.getElementById('#nota input');

// Eventos
eventListeners();
function eventListeners() {

    formulario.addEventListener('submit', validarcampos);

}
function validarcampos(e)
{
    
}